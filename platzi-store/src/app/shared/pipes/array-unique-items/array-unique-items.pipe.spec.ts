import { ArrayUniqueItemsPipe } from './array-unique-items.pipe';

describe('ArrayUniqueItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayUniqueItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
